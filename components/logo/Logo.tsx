/** @jsxImportSource @emotion/react */
import { css } from '@emotion/react';

function Logo() {
  return (
    <div
      css={css`
        font-size: 1.5rem;
        color: #333;
        font-weight: bold;
      `}
    >
      calog
    </div>
  );
}

export default Logo;
